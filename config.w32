// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("md_monitor", "for md_monitor support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("md_monitor", "enable md_monitor support", "no");

if (PHP_MD_MONITOR != "no") {
	EXTENSION("md_monitor", "md_monitor.c", PHP_EXTNAME_SHARED);
}

